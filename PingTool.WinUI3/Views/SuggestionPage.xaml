<Page
    x:Class="PingTool.Views.SuggestionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Send Suggestion" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Text="Have a feature request or found a bug? Let us know!" Margin="0,0,0,16" />

        <TextBox
            x:Name="SuggestionText"
            Grid.Row="2"
            PlaceholderText="Enter your suggestion or feedback here..."
            TextWrapping="Wrap"
            AcceptsReturn="True"
            MinHeight="200" />

        <Button
            Grid.Row="3"
            Content="Send Feedback"
            HorizontalAlignment="Right"
            Margin="0,16,0,0"
            Style="{StaticResource AccentButtonStyle}"
            Click="SendFeedback_Click" />
    </Grid>
</Page>
